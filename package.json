{
  "name": "poorchid",
  "version": "0.1.20",
  "description": "The poor man's Orchid - Open source chord synthesizer",
  "type": "module",
  "scripts": {
    "dev": "vite --port 8080 --host",
    "build": "vite build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "preview": "vite preview",
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^1.1.0",
    "gh-pages": "^6.3.0",
    "jsdom": "^24.0.0",
    "vite": "^5.0.0",
    "vitest": "^1.2.0"
  }
}
